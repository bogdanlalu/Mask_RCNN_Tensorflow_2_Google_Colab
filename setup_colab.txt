# For each Colab session, uncomment the code below and run it once 
# in the first cell of the notebook you are working in.

# Make sure you run from within the Mask_RCNN folder
# %cd Mask_RCNN/

# pip install -r requirements.txt
# git clone https://github.com/cocodataset/cocoapi.git && cd cocoapi/PythonAPI/ && make && make install && cd ../../
# !wget https://github.com/matterport/Mask_RCNN/releases/download/v1.0/mask_rcnn_coco.h5
